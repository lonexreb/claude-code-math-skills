Definition 1.3.6

**Definition 1.3.6.** Suppose _𝑓_ : _𝐷_ → ℝ is a function. We say _𝑓_ is _bounded_ if there exists a number _𝑀_ such that �� _𝑓_ ( _𝑥_ )�� ≤ _𝑀_ for all _𝑥_ ∈ _𝐷_ .

In the example, we proved _𝑥_[2] − 9 _𝑥_ + 1 is bounded when considered as a function on _𝐷_ = { _𝑥_ : −1 ≤ _𝑥_ ≤ 5}. On the other hand, if we consider the same polynomial as a function on the whole real line ℝ, then it is not bounded.

**==> picture [196 x 99] intentionally omitted <==**

**Figure 1.4:** Example of a bounded function, a bound _𝑀_ , and its supremum and infimum.

For a function _𝑓_ : _𝐷_ → ℝ, we write (see for an example)

**==> picture [290 x 22] intentionally omitted <==**

We also sometimes replace the “ _𝑥_ ∈ _𝐷_ ” with an expression. For example if, as before, _𝑓_ ( _𝑥_ ) = _𝑥_[2] − 9 _𝑥_ + 1, for −1 ≤ _𝑥_ ≤ 5, a little bit of calculus shows

**==> picture [410 x 24] intentionally omitted <==**