Definition 5.5.1

**Definition 5.5.1.** Suppose _𝑓_ : [ _𝑎, 𝑏_ ) → ℝ is a function (not necessarily bounded) that is Riemann integrable on [ _𝑎, 𝑐_ ] for all _𝑐 < 𝑏_ . We define

**==> picture [100 x 32] intentionally omitted <==**

if the limit exists.

Suppose _𝑓_ : [ _𝑎,_ ∞) → ℝ is a function such that _𝑓_ is Riemann integrable on [ _𝑎, 𝑐_ ] for all _𝑐 <_ ∞. We define

**==> picture [102 x 30] intentionally omitted <==**

if the limit exists.

If the limit exists, we say the improper integral _converges_ . If the limit does not exist, we . say the improper integral _diverges_

We similarly define improper integrals for the left-hand endpoint, we leave this to the reader.

For a finite endpoint _𝑏_ , if _𝑓_ is bounded, then says that we defined nothing new. What is new is that we can apply this definition to unbounded functions. The following set of examples is so useful that we state it as a proposition.

**Proposition 5.5.2** ( _𝑝_ -test for integrals) **.** _The improper integral_

**==> picture [56 x 30] intentionally omitted <==**

_converges to 𝑝_ −1 1 _[if][ 𝑝][>]_[1] _[ and diverges if]_[ 0] _[ <][𝑝]_[≤][1] _[.] The improper integral_

**==> picture [52 x 32] intentionally omitted <==**

1 _converges to_ 1− _𝑝[if]_[ 0] _[ <][𝑝][<]_[1] _[ and diverges if][ 𝑝]_[≥][1] _[.]_

_Proof._ The proof follows by application of the . Let us do the proof for _𝑝 >_ 1 for the infinite right endpoint and leave the rest to the reader. Hint: You should handle _𝑝_ = 1 separately.

_5.5. IMPROPER INTEGRALS_

215

Suppose _𝑝 >_ 1. Then using the fundamental theorem,

**==> picture [470 x 73] intentionally omitted <==**

We state the following proposition on “tails” for just one type of improper integral, though the proof is straightforward and the same for other types of improper integrals.