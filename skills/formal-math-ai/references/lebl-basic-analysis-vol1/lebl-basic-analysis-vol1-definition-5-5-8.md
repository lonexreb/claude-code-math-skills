Definition 5.5.8

**Definition 5.5.8.** Suppose _𝑓_ : ( _𝑎, 𝑏_ ) → ℝ is a function that is Riemann integrable on [ _𝑐, 𝑑_ ] for all _𝑐_ , _𝑑_ such that _𝑎 < 𝑐 < 𝑑 < 𝑏_ , then we define

**==> picture [130 x 32] intentionally omitted <==**

if the limits exist.

Suppose _𝑓_ : ℝ → ℝ is a function such that _𝑓_ is Riemann integrable on all bounded intervals [ _𝑎, 𝑏_ ]. Then we define

**==> picture [136 x 33] intentionally omitted <==**

if the limits exist.

We similarly define improper integrals with one infinite and one finite improper endpoint, we leave this to the reader.

One ought to always be careful about double limits. The definition given above says that we first take the limit as _𝑑_ goes to _𝑏_ or ∞ for a fixed _𝑐_ , and then we take the limit in _𝑐_ . We will have to prove that in this case it does not matter which limit we compute first.

## **Example 5.5.9:**

**==> picture [430 x 33] intentionally omitted <==**

Let us In the definition, the order of the limits can always be switched if they exist. state and prove this fact only for the limits at infinity.

_5.5. IMPROPER INTEGRALS_

219