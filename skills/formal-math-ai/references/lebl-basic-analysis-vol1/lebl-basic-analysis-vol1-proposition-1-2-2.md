Proposition 1.2.2

**Proposition 1.2.2.** _If 𝑥_ ∈ ℝ _is such that 𝑥_ ≤ _𝜖 for all 𝜖_ ∈ ℝ _where 𝜖>_ 0 _, then 𝑥_ ≤ 0 _._

_Proof._ If _𝑥 >_ 0, then 0 _<[𝑥]_ /2 _< 𝑥_ (why?). Take _𝜖_ = _[𝑥]_ /2 to get a contradiction. Thus _𝑥_ ≤ 0. □

For nonnegative _𝑥_ , equality results: _If 𝑥_ ≥ 0 _is such that 𝑥_ ≤ _𝜖 for all 𝜖>_ 0 _, then 𝑥_ = 0 _._ A common version uses the absolute value (see ): _If_ | _𝑥_ | ≤ _𝜖 for all 𝜖>_ 0 _, then 𝑥_ = 0 _._ To prove _𝑥_ ≥ 0, an analyst might prove that _𝑥_ ≥− _𝜖_ for all _𝜖>_ 0. From now on, when we say _𝑥_ ≥ 0 or _𝜖>_ 0, we automatically mean that _𝑥_ ∈ ℝ and _𝜖_ ∈ ℝ.

The idea behind the proposition above is that any time we have two real numbers _𝑎 < 𝑏_ , there is another real number _𝑐_ such that _𝑎 < 𝑐 < 𝑏_ . _𝑐_ exist. One of Infinitely many such them is, for example, _𝑐_ = _[𝑎]_[+] 2 _[𝑏]_[(why?).][We will use this fact in the next example.]

The most useful property of ℝ for analysts is not just that it is an ordered field, but that it has the . Essentially, we want ℚ, but we also want to take suprema (and infima) willy-nilly. So what we do is take ℚ and throw in enough numbers to obtain ℝ. We mentioned already that ℝ contains elements that are not in ℚ because of the . Let us prove it. We saw there is no rational square root of two. The set { _𝑥_ ∈ ℚ : _𝑥_[2] _<_ 2} implies the existence of the real number ~~√~~ 2, although this fact requires a bit of work. See also .